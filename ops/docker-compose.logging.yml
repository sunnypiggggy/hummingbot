services:
  caddy:
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
  v2ray:
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
